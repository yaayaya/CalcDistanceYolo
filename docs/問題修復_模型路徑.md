# 🔧 問題修復: YOLO 模型路徑找不到

## ❌ 問題描述

```
FileNotFoundError: 找不到 YOLO 模型檔案: yolo11n.pt
```

## 🔍 原因分析

原本的 `get_model_path()` 函式搜尋順序有誤:
1. ❌ `backend/../models/yolo11n.pt` (專案中沒有 models 資料夾)
2. ❌ `backend/yolo11n.pt` (模型不在 backend 資料夾)
3. ❌ `backend/../tools/基本偵測/yolo11n.pt` (也不在這裡)

實際模型位置: ✅ `專案根目錄/yolo11n.pt`

## ✅ 修復方案

修改 `backend/app/utils/config_loader.py` 中的 `get_model_path()` 函式:

### 修正後的搜尋順序:
1. ✅ **專案根目錄** (`BASE_DIR.parent / model_filename`)
2. models 資料夾 (`BASE_DIR.parent / "models" / model_filename`)
3. backend 資料夾 (`BASE_DIR / model_filename`)
4. 工具資料夾 (`BASE_DIR.parent / "tools" / "基本偵測" / model_filename`)

### 程式碼變更:

```python
def get_model_path() -> Path:
    """取得 YOLO 模型路徑"""
    sensor_config = load_sensor_config()
    model_filename = sensor_config["model"]["model_path"]
    
    # 搜尋路徑列表 (依優先順序)
    search_paths = [
        BASE_DIR.parent / model_filename,                    # 專案根目錄 ⭐
        BASE_DIR.parent / "models" / model_filename,         # models 資料夾
        BASE_DIR / model_filename,                           # backend 資料夾
        BASE_DIR.parent / "tools" / "基本偵測" / model_filename,  # 工具資料夾
    ]
    
    # 依序尋找模型檔案
    for model_path in search_paths:
        if model_path.exists():
            return model_path
    
    # 找不到模型檔案,列出所有嘗試過的路徑
    searched_paths = "\n  - ".join(str(p) for p in search_paths)
    raise FileNotFoundError(
        f"找不到 YOLO 模型檔案: {model_filename}\n"
        f"已搜尋以下路徑:\n  - {searched_paths}"
    )
```

## 📊 測試結果

修復後服務成功啟動:

```
✅ 服務啟動完成!
📍 後台管理介面: http://localhost:8000/admin
📍 API 文件: http://localhost:8000/docs
```

## 💡 優化說明

1. **優先搜尋專案根目錄**: 因為這是最常見的模型存放位置
2. **詳細錯誤訊息**: 如果找不到模型,會列出所有嘗試過的路徑,方便除錯
3. **向下相容**: 保留其他搜尋路徑,確保不同專案結構都能正常運作

## ✅ 修復完成

問題已解決,服務可正常啟動並載入 YOLO 模型!
