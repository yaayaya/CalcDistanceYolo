# 🚀 快速啟動指南

## 第一次使用

### 1️⃣ 安裝依賴套件
```powershell
pip install -r requirements.txt
```

### 2️⃣ 校準焦距 (重要!)
```powershell
cd 基本偵測
python camera_test_gui_v2.py
```

**校準步驟:**
1. 點擊「▶ 啟動偵測」
2. 站在已知距離處 (例如 200cm)
3. 直接點擊畫面上的人物
4. 輸入實際距離 (200)
5. 點擊「執行快速校準」
6. 點擊「💾 儲存焦距」
7. 點擊「💾 儲存所有設定」

### 3️⃣ 啟動 FastAPI 服務
```powershell
cd ..
python main.py
```

### 4️⃣ 開啟後台監控
瀏覽器開啟: http://localhost:8000/admin

---

## 日常使用

### 啟動服務
```powershell
# 在 Test 資料夾下執行
python main.py
```

### 存取服務
- **後台監控**: http://localhost:8000/admin
- **API 文件**: http://localhost:8000/docs
- **健康檢查**: http://localhost:8000/health

### 停止服務
在終端機按 `Ctrl + C`

---

## 常用操作

### 重新校準焦距
1. 停止 FastAPI 服務 (`Ctrl + C`)
2. 執行 `python 基本偵測/camera_test_gui_v2.py`
3. 進行校準並儲存
4. 重新啟動 FastAPI 服務
5. 在後台點擊「🔁 重啟偵測器」

### 修改網路設定
1. 開啟後台 http://localhost:8000/admin
2. 在「網路設定」區塊修改參數
3. 點擊「💾 儲存設定」
4. 點擊「🔄 刷新 WebSocket 連線」

### 檢視即時資料
開啟後台即可看到:
- ✅ 偵測人數
- 📏 最近距離 (含顏色標示)
- ⚡ FPS (目標/實際)
- ⏱️ 運行時間

---

## 前端作品串接

### WebSocket 方式 (推薦)
```javascript
const ws = new WebSocket('ws://localhost:8000/ws/live');

ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    console.log('最近距離:', data.closest_distance);
    console.log('偵測人數:', data.total_count);
};
```

### REST API 方式 (輪詢)
```javascript
async function getDistance() {
    const res = await fetch('http://localhost:8000/api/distance/current');
    const result = await res.json();
    console.log(result.data);
}

setInterval(getDistance, 100); // 每 100ms 更新
```

---

## 疑難排解

### ❌ 攝影機無法開啟
- 檢查攝影機是否被其他程式佔用
- 修改 `sensor_config.json` 的 `camera.source` (0 或 1)

### ❌ 距離不準確
- 重新執行焦距校準
- 確認人物站立姿態清楚
- 建議多點校準 (3-5 個距離點)

### ❌ FPS 太低
- 降低 `sensor_config.json` 的 `model.imgsz` (改為 320)
- 增加 `model.vid_stride` (改為 5)
- 啟用 GPU (需安裝 CUDA)

### ❌ WebSocket 斷線
- 後台點擊「🔄 刷新 WebSocket 連線」
- 或重新整理後台頁面

---

## 檔案說明

| 檔案 | 說明 | 修改方式 |
|------|------|---------|
| `sensor_config.json` | 感測器配置 | 使用 GUI 工具 |
| `configs/network_config.json` | 網路配置 | 透過後台修改 |
| `main.py` | 主程式 | 程式碼修改 |
| `基本偵測/camera_test_gui_v2.py` | 校準工具 | 直接執行 |

---

**🎉 完成!現在可以開始使用了!**
